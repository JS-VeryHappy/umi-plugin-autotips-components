.main {
    position: relative;
    background:rgba(255,255,255,.3);
    .icon {
        position: absolute;
        top: 2px;
        left: -15px;
        font-size: 12px;
        color: #1890ff;
        cursor: pointer;
        z-index: 999;
    }
}